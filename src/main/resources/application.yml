
server:
  port: 8083
spring:
  application:
    name: BILLING-SERVICE
  h2:
    console:
      enabled: true
  cloud:
    discovery:
      enabled: false
  datasource:
    url: jdbc:h2:mem:billing-db

